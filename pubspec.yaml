name: dailymotivationalquotesapp
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.9.0-196.0.dev

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.3
  share_plus: ^12.0.0
  provider: ^6.1.5+1
  path_provider: ^2.1.1
  permission_handler: ^11.4.0
  gal: ^2.3.0
  flutter_local_notifications: ^19.4.2
  timezone: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/quotes.json